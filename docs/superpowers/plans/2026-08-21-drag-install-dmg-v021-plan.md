# 社区AI管理系统 v0.2.1 拖拽安装包实施计划

> 说明：当前工作区没有可调用的 `writing-plans` 技能，因此按同等粒度记录实施计划。

1. 将应用版本从 `0.2.0` 更新为 `0.2.1`，同步锁文件版本。
2. 调整 arm64 构建脚本：保留运行时应用准备、去扩展属性和签名校验；移除 PKG 根目录与 `pkgbuild` 步骤。
3. 在发布目录创建 DMG 临时内容：复制 `社区AI管理系统.app`，并创建指向 `/Applications` 的快捷入口。
4. 用压缩 DMG 格式生成 `社区AI管理系统-0.2.1-arm64.dmg`，清理临时目录。
5. 继续生成稳定英文命名的自动更新 ZIP 与 `latest-mac.yml`，确保清单的路径、大小和摘要与上传资产一致。
6. 运行测试、构建 arm64 安装包、验证代码签名，挂载 DMG 检查内容后卸载。
7. 仅提交本次版本与打包相关文件并推送；创建 GitHub `v0.2.1` Release，上传 DMG、ZIP 和更新清单。
