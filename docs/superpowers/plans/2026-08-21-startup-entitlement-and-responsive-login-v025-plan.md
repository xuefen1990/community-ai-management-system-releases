# v0.2.5 启动授权与响应修复实施计划

1. 在本地认证服务中提供不创建会话的上次账号授权摘要，并通过受限 IPC/预加载接口暴露给渲染层。
2. 让本地认证脚本先于旧版兼容脚本启动；授权摘要为有效授权时，仅安装短期新增节点/文本监听，删除旧试用弹窗及遮罩节点后立即结束。
3. 删除 v0.2.4 的全页面永久观察器、滤镜覆盖和反复强制页面切换；登录页只在启动/退出登录时切换一次。
4. 增加服务、预加载和渲染层自动化覆盖，保证历史授权不自动登录、旧节点被删除且没有全页样式监听。
5. 更新版本至 v0.2.5，运行完整测试，构建并挂载验证 arm64 拖拽安装 DMG。
6. 仅提交本次文件，推送当前分支并发布 v0.2.5 安装文件。
