# 实施计划

1. 隔离工作台数据转换、居民候选、复用规则、版式和分页算法，编写纯函数测试。
2. 接入正式批次入口，实现表格式编辑、姓名候选、自动计算、草稿保存与撤销，保留导入数据。
3. 接入打印预览：可调字体/单元格/行列/边距，实测分页，复用同一输出用于打印。
4. 接通模板保存、导出和现有发放完成核对，确保只有完成时写居民资料。
5. 执行针对性、全量与界面验证，检查本次差异，修复发现的问题。
6. 仅提交本次文件，升级版本及发行说明，推送当前分支与对应标签，核对发布附件与更新后端。

writing-plans 技能当前未安装，使用本文件记录实施与验收步骤。已确认方案不再重复暂停询问。

## 实施记录

- 第 1–4 步完成：独立数据规则与编辑器接入正式菜单，保留现有居民完成同步接口。
- 第 5 步：324 项全量测试通过；隔离 Chromium 交互测试及真实菜单适配测试通过。618 人长文本按实际高度分页，编辑只挂载 20 行，预览仅挂载一张纸。虚拟时钟不用于宣称实际耗时。
- 第 6 步：准备 v0.3.57 发行说明和版本校验；发布附件与后端版本需在推送标签后分别验证。
