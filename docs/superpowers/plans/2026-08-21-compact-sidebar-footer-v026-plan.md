# v0.2.6 紧凑侧边栏底部实施计划

1. 将底部操作区改为两行：第一行放置刷新额度和安全退出，第二行放置授权同步与检查更新。
2. 保留 `syncTokenBtn`、`logoutBtn`、`manualUpdateCheckBtn` 的标识、标题与既有事件，仅调整呈现层级和文字。
3. 更新 CSS，使账户区、主操作和次级操作使用紧凑且不截断的响应式布局，并兼容明暗主题与键盘聚焦状态。
4. 将版本升级为 `0.2.6`，更新布局测试以验证四个入口及两行编排。
5. 运行完整测试，构建并检查可拖入“应用程序”的 Apple Silicon DMG。
6. 仅提交本次改动、推送当前分支，并发布 v0.2.6 安装包。
