# v0.3.52

## AI 助理修复

- macOS 安全存储暂时不可用时，本机台账查询不会被在线 AI 密钥读取阻断。
- 重新输入的在线 AI API 密钥只保存在本次打开软件的内存中，关闭软件后自动清除，不会以明文写入电脑。
- 对需要在线理解的复杂问题，助理会明确提示前往“系统设置 → AI 配置”重新输入密钥，而不再显示无法处理的系统内部错误。

## 同步包含

- 包含已提交的资金发放模板与打印流程优化。
