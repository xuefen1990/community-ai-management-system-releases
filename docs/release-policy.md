# 客户端更新发布约束

从 2026-08-24 起，任何影响前端或后端的正式版本更新都必须满足以下要求：

1. 生成 macOS 安装包、应用内更新 ZIP 与更新清单。
2. 同步发布到 GitHub Release，供人工下载与版本留档。
3. 将同一版本的 ZIP 和更新说明登记到更新后端。
4. 发布前以旧版本号调用后端更新检查接口，确认客户端能发现新版本、获得可下载地址并显示可点击的更新操作。
5. 若任一环节失败，不得宣称该版本已发布；应修复或明确报告失败原因。

这项约束适用于之后所有新窗口、前端和后端的版本更新。
