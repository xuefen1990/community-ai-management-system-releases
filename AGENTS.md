# 项目协作约定

## 完成工作后的 GitHub 上传

- 完成本次用户要求的代码或文档修改后，先检查 `git status` 与差异，并运行与本次改动相关的验证。
- 只暂存和提交本次对话实际修改的文件；不得把已有的、与本次无关的未提交改动一并提交。
- 验证通过后，在当前分支创建清晰的提交，并推送到该项目当前的 `origin` 远程仓库。
- 不使用强制推送，不重置或覆盖已有改动。
- 如果发现合并冲突、认证问题、远程拒绝或验证失败，停止推送并向用户说明原因和可选处理方式。

## GitHub 持久授权与应用更新发布

- 本机 GitHub 凭据首次授权后，使用 macOS 钥匙串中的 Git 凭据复用；新开的 Codex 窗口或项目对话不得要求用户重复授权。
- 推送前可运行 `npm --prefix app run github:verify`。该检查禁止交互式登录；凭据失效时才提示一次性恢复操作，不得在脚本或仓库中保存令牌。
- 每次普通改动仍须提交并推送；只有创建与 `app/package.json` 版本一致的 `v<version>` 标签时，GitHub Actions 才构建并发布可由客户端“检查更新”安装的版本。

## 已安装 App 的自动更新发布（长期约束）

- 用户已明确授权：凡是会影响已安装“社区AI管理系统”功能、界面、数据或稳定性的完成改动，均视为一次面向用户的应用更新；不得只推送代码后要求用户手动下载 DMG 或重复交代发布要求。
- 完成此类改动后，除常规提交与分支推送外，必须自行完成下一版语义化版本号、`docs/releases/<version>.md` 发行说明、与 `app/package.json` 一致的 `v<version>` 标签，并推送该标签触发发布工作流。
- 发布成功的标准是：GitHub Release 同时包含 DMG、应用内 ZIP 更新包与 `latest-mac.yml`，且更新后端已确认该版本。只有满足该标准，才能告知用户可在 App 中点击“检查更新”，再按“立即更新 → 重启并安装”完成升级。
- 除非用户明确要求，用户后续不需要再次提醒、手动下载安装包或重复授权。仅文档、开发工具或不影响已安装 App 的内部改动可不创建应用更新版本。
- 若发布工作流、版本校验或更新后端同步失败，必须明确说明失败原因和恢复步骤；不得把“代码已推送”当作可在 App 内更新。
